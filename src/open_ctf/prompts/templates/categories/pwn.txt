# PWN CHALLENGE - IMPORTANT
This is a binary exploitation challenge. The remote service runs at {raw_target} (raw TCP, NOT HTTP). Examine source code and binaries in /root/challenge/ if available, then interact with the remote service using netcat or Python (pwntools):
  shell_command(command="nc {nc_target}")